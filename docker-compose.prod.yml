
services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    image: myapp-app:prod
    ports:
      - "${APP_PORT:-80}:3000"
    environment:
      - NODE_ENV=production
      - PORT=3000
      - HOSTNAME=0.0.0.0
    restart: unless-stopped
    volumes: []   
